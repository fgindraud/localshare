language: cpp

matrix:
   include:
      - env: PLATFORM='linux'
        os: linux
        dist: trusty
        sudo: required

      - env: PLATFORM='mac'
        os: osx
        osx_image: xcode7.2

   allow_failures:
      - os: osx

before_install:
   - source ./build/${PLATFORM}/requirement.sh

install:
   - qmake
   - make

script:
   - echo "Ok"

